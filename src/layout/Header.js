import React from "react";
import MainMenu from '../components/MainMenu.js';

const Header = (props) => {

    return (
      <header>
        <MainMenu/>
      </header>
    );
}


export default Header;